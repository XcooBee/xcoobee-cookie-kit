{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "lint": "lerna run lint",
    "__comment__test": "Ignoring the React example package because it runs its tests in interactive mode, and it is a little bit of a pain to disable interactive mode.",
    "test": "lerna run test --ignore xcoobee-cookie-kit-example-react",
    "web:clear": "lerna run clear --scope xcoobee-cookie-kit-web",
    "web:build": "lerna run build --scope xcoobee-cookie-kit-web"
  },
  "dependencies": {
    "react": "16.2",
    "react-dom": "16.2.1"
  },
  "devDependencies": {
    "lerna": "^3.1.4"
  }
}
