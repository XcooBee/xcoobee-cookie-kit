hooks:
  pre_setup: npm install
  post_build: npm run build && npm run clear
nodejs:
  version: '8.10.0'
tests:
  - npm test
environment:
  PORT: '3000'
  XBEE_STAGE: 'test'
  NODE_ENV: 'development'
  CI: 'true'
timeout_hook: 900
