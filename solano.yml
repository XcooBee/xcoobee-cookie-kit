hooks:
  pre_setup: npm install && npx lerna bootstrap
  post_build: npm run web:build && npm run web:clear
nodejs:
  version: '8.10.0'
tests:
  - npm web:test
environment:
  PORT: '3000'
  XBEE_STAGE: 'test'
  NODE_ENV: 'development'
  CI: 'true'
timeout_hook: 900
