hooks:
  pre: npm install
  post_build: npm run solanobuild && npm prune --production && npm run clear
nodejs:
  version: '6.10.0'
tests:
  - npm test
environment:
  PORT: '3000'
  XBEE_STAGE: 'test'
  NODE_ENV: 'production'
  CI: 'true'
timeout_hook: 900